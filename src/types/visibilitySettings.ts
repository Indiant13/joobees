export type VisibilitySettingsDTO = {
  showAvatar: boolean;
  showBio: boolean;
  showLocation: boolean;
  showTimezone: boolean;
  showLinks: boolean;
  showBadges: boolean;
  showStats: boolean;
};
